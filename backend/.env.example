# Port the Bodha AI REST API listens on
PORT=4000

# Comma-separated list of browser origins allowed to call the API
CORS_ORIGIN=http://localhost:5173,http://localhost:4000,https://localhost,capacitor://localhost

# Absolute or relative path to the SQLite database file (created on first run)
DATABASE_PATH=./data/bodha.db

# MongoDB connection URI (optional - if set, Bodha AI connects to MongoDB Atlas or local Mongo)
# Example: mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/bodha_ai?retryWrites=true&w=majority
MONGODB_URI=

# --- Listing optimizer -------------------------------------------------------
# Leave LLM_API_KEY empty to use the deterministic rule-based listing optimizer.
# Supplying a key is the single switch needed to route through a real LLM once
# an LLM-backed implementation is registered in services/listingOptimizer.ts.
LLM_API_KEY=
LLM_MODEL=claude-sonnet-5

# Gemini powers language-aware listing copy when set.
# Leave empty to use template fallbacks.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# ElevenLabs Conversational AI (existing agent — do not create a new one).
# Required on the server to mint a conversation token / signed URL for a
# private agent. Never commit a real key. The Agent ID is public.
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=agent_3301m2486d2qecasbeht51m4ef4t
ELEVENLABS_BRANCH_ID=agtbrch_7501m2486ek8erka0cw0dy0y87d2
# Shared secret checked on every /api/voice-tools/* call. Leave empty in local
# dev; before deploying, set this AND configure the same value as a custom
# "x-tool-secret" header on each server tool in the ElevenLabs dashboard —
# otherwise anyone who finds the URL and a productId can read that seller's
# stored report.
ELEVENLABS_TOOL_SECRET=

# --- Live marketplace scrape -------------------------------------------------
# How long a scrape is reused before the next analyze hits the live page.
CACHE_TTL_HOURS=3
# Per-platform Playwright timeout (ms)
SCRAPE_TIMEOUT_MS=25000
# Cap of comparable cards extracted from page 1
SCRAPE_MAX_RESULTS=20
SCRAPE_HEADLESS=true

# --- Accounts & Razorpay test checkout --------------------------------------
# $10 / month Pro plan is billed as ₹830 (83000 paise) in Razorpay test mode.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
PRO_PRICE_PAISE=83000
FREE_ANALYSES_PER_MONTH=6

# --- "Sign in with Google" ---------------------------------------------------
# OAuth 2.0 Client ID from https://console.cloud.google.com/apis/credentials
# (Create Credentials -> OAuth client ID -> Web application). Add your site's
# URL under "Authorized JavaScript origins" (e.g. http://localhost:5173 for
# dev). This is the SAME value as frontend/.env's VITE_GOOGLE_CLIENT_ID — it's
# a public identifier, not a secret. Leave empty to disable Google sign-in.
GOOGLE_CLIENT_ID=
